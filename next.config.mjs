/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media-assets.swiggy.com",
      "img.spoonacular.com",
      "spoonacular.com",
    ],
  },
};

export default nextConfig;
